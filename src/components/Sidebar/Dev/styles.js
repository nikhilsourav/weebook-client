import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  Dev: {
    position: 'absolute',
    bottom: '20px',
    left: '50%',
    transform: 'translateX(-50%)',
  },
}));
